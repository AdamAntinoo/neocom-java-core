<?xml version="1.0" encoding="UTF-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android" >

    <!-- Define the background properties like color etc -->
    <item android:id="@android:id/background">
        <padding
            android:bottom="1px"
            android:left="1px"
            android:right="1px"
            android:top="1px" />

        <solid android:color="@color/orangeDarkBkgnd" />

        <stroke
            android:width="1px"
            android:color="@color/orangeBrightLine" />
    </item>

    <!-- Define the progress properties like start color, end color etc -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <gradient
                    android:angle="180"
                    android:centerColor="@color/blueDarkBkgnd"
                    android:centerY="1.0"
                    android:endColor="@color/blueDarkBkgnd"
                    android:startColor="@color/blueBrightBkgnd" />
            </shape>
        </clip>
    </item>

</layer-list>