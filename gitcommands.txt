
--------------------------------------------------
git for-each-ref --format="%(refname)" refs/original/ | xargs -n 1 git update-ref -d
git reflog expire --expire=now --all


$ rm -rf .git/refs/original/ 
$ git update-ref -d refs/original/
$ git gc
$ rm -Rf .git/refs/original
$ rm -Rf .git/logs/
$ git gc
git count-objects -vH

--------------------------------------------------

git filter-branch -f --prune-empty  --index-filter "git rm --cached -r -f --ignore-unmatch sqlitebrowser_200_b1_win AndroidCoreLibrary DroidMarketManager SiriRestClient-master net.sf.sandbox.rcpapplication net.sf.sandbox.draftJPA TapCounter MavenBase BlueprintListProof DroidUI01 ProofAssesProcessing ProofCharacterSelection ProofMultiCharacter DroidMarket net.sf.sandbox.static.GenericWeb PapyrusExample Firefox-OS-Boilerplate-App-gh-pages SQLite ObserverExample AndroidPersistence" --tag-name-filter cat -- --all



AndroidPersistence/sdcard.img



git-fatfiles:

git rev-list --all --objects | \
    sed -n $(git rev-list --objects --all | \
    cut -f1 -d' ' | \
    git cat-file --batch-check | \
    grep blob | \
    sort -n -k 3 | \
    tail -n40 | \
    while read hash type size; do 
         echo -n "-e s/$hash/$size/p ";
    done) | \
    sort -n -k1
...
89076 images/screenshots/properties.png
103472 images/screenshots/signals.png
9434202 video/parasite-intro.avi
git-eradicate (for video/parasite.avi):

git filter-branch -f  --index-filter \
    'git rm --force --cached --ignore-unmatch video/parasite-intro.avi' \
     -- --all
rm -Rf .git/refs/original && \
    git reflog expire --expire=now --all && \
    git gc --aggressive && \
    git prune
    
    